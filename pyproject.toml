[project]
name = "internship-recommendation-engine-for-pm-intership-scheme"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Pritam Pramanik", email = "70059343+Radheya26@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastembed>=0.8.0",
    "langchain>=1.3.15",
    "langgraph>=1.2.11",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
    "streamlit>=1.61.1",
]

[project.scripts]
internship-recommendation-engine-for-pm-intership-scheme = "internship_recommendation_engine_for_pm_intership_scheme:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
